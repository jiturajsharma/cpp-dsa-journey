#include<iostream>
using namespace std;

int main(){
    float a = 5, b = 2;
    int c;
    c = a%b;
    cout<<c<<endl;
    // The modulus operator (%) is not defined for floating-point numbers in C++.
    // Uncommenting the above line will result in a compilation error.

    float x = 5.0, y = 2.0;
    int e;
    e = x%y;
    cout<<e<<endl;
    // The modulus operator (%) is not defined for floating-point numbers in C++.
    // Uncommenting the above line will result in a compilation error.

    float p = 5.0, q = 2;
    int t;
    t = p%q;
    cout<<t<<endl;
    // The modulus operator (%) is not defined for floating-point numbers in C++.
    // Uncommenting the above line will result in a compilation error.


    float ar = 5, br = 2;
    int crr;
    crr = ar%br;
    cout<<crr<<endl;
    // The modulus operator (%) is not defined for floating-point numbers in C++.
    // Uncommenting the above line will result in a compilation error.
}