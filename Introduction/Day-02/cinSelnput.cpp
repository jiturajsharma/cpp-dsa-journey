#include<iostream>
using namespace std;

int main(){
    //  are of circle take input from user only radius
    float radius, area;
    cout<<"Enter the radius of circle: ";
    cin>>radius;
    area = 3.14 * radius * radius;
    cout<<"Area of circle is: "<<area;

    int x;
    cin>>x;
    cout<<"You entered: "<<x;

}